<div class="container my-4">
  <h1 class="text-center mb-4">📦 Catálogo de Productos</h1>

  <div class="row">
    {{#each products}}
      <div class="col-md-4 mb-4">
        <div class="card h-100 shadow-sm">
          {{#if this.thumbnails.[0]}}
            <img src="{{this.thumbnails.[0]}}" class="card-img-top" alt="{{this.title}}">
          {{else}}
            <img src="https://via.placeholder.com/300x200?text=Sin+Imagen" class="card-img-top" alt="Sin imagen">
          {{/if}}
          <div class="card-body d-flex flex-column">
            <h5 class="card-title">{{this.title}}</h5>
            <p class="card-text">{{this.description}}</p>
            <p class="fw-bold mb-1">💲 {{this.price}}</p>
            <p class="text-muted mb-2">Categoría: {{this.category}}</p>
            <p class="text-muted mb-2">Stock: {{this.stock}}</p>
            <div class="mt-auto">
              <a href="/products/{{this._id}}" class="btn btn-sm btn-outline-primary w-100 mb-2">Ver Detalles</a>
      <form action="/api/carts/68db14911e8d4256c5dc5e9b/products/{{this._id}}" method="post">
        <button class="btn btn-sm btn-success w-100" type="submit">Agregar al carrito</button>
      </form>
            </div>
          </div>
        </div>
      </div>
  {{/each}}
</div>

  <nav aria-label="Page navigation">
    <ul class="pagination justify-content-center">
  {{#if hasPrevPage}}
    <li class="page-item">
          <a class="page-link" href="{{prevLink}}">Anterior</a>
        </li>
  {{/if}}
  <li class="page-item active">
        <span class="page-link">{{page}} de {{totalPages}}</span>
      </li>
  {{#if hasNextPage}}
    <li class="page-item">
          <a class="page-link" href="{{nextLink}}">Siguiente</a>
        </li>
  {{/if}}
    </ul>
  </nav>
</div>
